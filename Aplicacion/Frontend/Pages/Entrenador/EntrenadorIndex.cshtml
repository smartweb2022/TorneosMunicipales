@page
@model Frontend.Pages.EntrenadorIndexModel
@{
    ViewData["Title"]= "EntrenadorIndex";
}
<h1>Lista de Entrenadores</h1>
<p> 
    <a asp-page= "Create"> Crear Entrenador</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th> Id </th>   
            <th> Nombre </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var Entre in Model.Entrenadores)
        {
        <tr>
            <td> @Entre.Id </td>
            <td> @Entre.Nombres </td>
            <td>
                <a asp-page="./EditMuni" asp-route-id="@Entre.Id">Editar</a>
                <a asp-page="./DeleteMuni"asp-route-id="@Entre.Id">Eliminar</a>
                <a asp-page="./DetailsMuni"asp-route-id="@Entre.Id">Detalles</a>
            </td>
        </tr>
        }
    </tbody>
</table>
